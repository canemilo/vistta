{
  "name": "vistta-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "pnpm typecheck && pnpm lint && pnpm format:check && pnpm test",
    "db:create": "wrangler d1 create vistta",
    "db:migrate:local": "wrangler d1 migrations apply vistta --local",
    "db:migrate:remote": "wrangler d1 migrations apply vistta --remote",
    "db:seed:local": "wrangler d1 execute vistta --local --file=seed/demo.sql",
    "user:create": "node scripts/crear-usuario.mjs",
    "seed:cuentas": "node scripts/crear-usuario.mjs nordeste \"Estudio Nordeste\" demo-vistta-2026 && node scripts/crear-usuario.mjs marina \"Marina Ruiz\" demo-vistta-2026",
    "seed:fotos": "bash seed/fotos.sh",
    "setup:local": "pnpm db:migrate:local && pnpm seed:cuentas && pnpm db:seed:local && pnpm seed:fotos",
    "dev:api": "wrangler dev",
    "dev:web": "pnpm --dir web start",
    "dev:free": "lsof -ti:8787,4200 | xargs kill 2>/dev/null || true",
    "dev:all": "pnpm dev:free && concurrently -n api,web -c green,cyan --kill-others-on-fail \"pnpm dev:api\" \"pnpm dev:web\""
  },
  "dependencies": {
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241127.0",
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.5",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "~2.1.8",
    "wrangler": "^3.95.0"
  }
}
